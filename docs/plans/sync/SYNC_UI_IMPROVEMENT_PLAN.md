# План покращення UI синхронізації

## 📋 Аналіз поточного стану

### Що є зараз:
1. ✅ Запит 60.1 з обмеженням 45 днів
2. ✅ Завантаження деталей 61.1 для вибраних МД
3. ⚠️ Прогрес-бар тільки для 60.1 (показується під час завантаження)
4. ⚠️ Кнопка 60.1 маленька (не на всю ширину)
5. ❌ Немає видимого прогресу для 61.1 (коли їх багато)
6. ❌ Немає можливості завантажити весь доступний період автоматично
7. ❌ Немає глобального прогрес-бара (видимого на всіх сторінках)

### Обмеження API:
- Запит 60.1: **максимум 45 днів за один запит**
- Загальний період: **теперішній + 3 попередні роки або 1095 днів** (близько 3 роки)

---

## 🎯 План реалізації

### Етап 1: Покращення UI елементів (Базові зміни)

#### 1.1 Кнопка 60.1 на всю ширину
- [ ] Змінити розмір кнопки "Запит 60.1 (Список)" на всю ширину контейнера
- [ ] Покращити візуальне оформлення кнопки

#### 1.2 Покращення відображення прогресу для 61.1
- [ ] Додати детальний прогрес-бар для завантаження деталей 61.1
- [ ] Показувати: "Завантажено X з Y" для 61.1
- [ ] Додати можливість відмінити процес завантаження 61.1

---

### Етап 2: Завантаження великого періоду (Автоматичне розбиття)

#### 2.1 Новий режим "Завантажити весь період"
- [ ] Додати кнопку "Завантажити весь доступний період"
- [ ] Автоматично розбивати період на чанки по 45 днів
- [ ] Послідовно виконувати запити для кожного чанку

**Алгоритм розбиття:**
```
Загальний період: 1095 днів (3 роки)
Чанки: 1095 / 45 = ~24 запити

1. Розбити на чанки: [День 1-45], [День 46-90], ..., [День 1051-1095]
2. Послідовно виконувати кожен запит
3. Показувати прогрес: "Завантажено період X-Y (N з M)"
```

#### 2.2 Логіка автоматичного розбиття
- [ ] Функція `splitPeriodIntoChunks(startDate, endDate, maxDays = 45)`
- [ ] Повертає масив періодів: `[{ start, end }, ...]`
- [ ] Використовувати для автоматичного завантаження

---

### Етап 3: Глобальний прогрес-бар (Багатосторінковий)

#### 3.1 Компонент глобального прогрес-бара
- [ ] Створити компонент `GlobalSyncProgressBar`
- [ ] Розмістити в `app/layout.tsx` або `app/dashboard/layout.tsx`
- [ ] Використовувати Context API або Zustand для стану

**Розташування:**
```
Верхня панель (header) або нижня панель (footer)
Видима на всіх сторінках дашборду
```

#### 3.2 State Management для глобального прогресу
- [ ] Створити Context `SyncProgressContext`
- [ ] Можливі стани:
  ```typescript
  {
    isActive: boolean;
    currentStep: number;
    totalSteps: number;
    currentPeriod?: { start: Date; end: Date };
    currentAction: string;
    canCancel: boolean;
  }
  ```

#### 3.3 Інтеграція з синхронізацією
- [ ] Оновити `syncDeclarations` для підтримки прогрес-зворотного виклику (callback)
- [ ] Оновити `fetchDeclarationDetails` для показу прогресу
- [ ] Додати можливість відмінити процес

---

### Етап 4: UI компоненти та інтерфейс

#### 4.1 Покращена панель синхронізації
- [ ] Додати кнопку "Завантажити весь період" поряд з існуючою
- [ ] Додати індикатор активних завантажень
- [ ] Показувати кількість чанків та поточний чанк

**Макет:**
```
┌─────────────────────────────────────────┐
│  Запуск Синхронізації                   │
├─────────────────────────────────────────┤
│  [Дата від] [Дата до]                   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Запит 60.1 (Список)            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Завантажити весь період        │   │
│  │  (автоматично розбити на чанки) │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [Прогрес для поточного запиту]        │
└─────────────────────────────────────────┘
```

#### 4.2 Глобальний прогрес-бар (дизайн)
- [ ] Компактний прогрес-бар в верхній частині екрана
- [ ] Показувати: "Синхронізація: Завантаження періоду 01.01.2025 - 15.02.2025 (5/24)"
- [ ] Кнопка "Скасувати" (якщо `canCancel`)
- [ ] Автоматично ховається після завершення

**Розташування:**
```
Верхня панель під header (sticky)
Або нижня панель (sticky to bottom)
```

---

## 🔧 Технічна реалізація

### Нові файли:
1. `src/contexts/SyncProgressContext.tsx` - Context для глобального прогресу
2. `src/components/GlobalSyncProgressBar.tsx` - Компонент глобального прогрес-бара
3. `src/app/dashboard/sync/utils/period-splitter.ts` - Утиліта для розбиття періоду

### Оновлені файли:
1. `src/app/dashboard/sync/page.tsx` - Покращений UI
2. `src/actions/sync.ts` - Підтримка callback для прогресу
3. `src/app/dashboard/layout.tsx` - Додати GlobalSyncProgressBar

### Функції:

#### `splitPeriodIntoChunks(startDate, endDate, maxDays = 45)`
```typescript
function splitPeriodIntoChunks(startDate: Date, endDate: Date, maxDays: number = 45) {
    const chunks: Array<{ start: Date; end: Date }> = [];
    let currentStart = new Date(startDate);
    
    while (currentStart < endDate) {
        const currentEnd = new Date(currentStart);
        currentEnd.setDate(currentEnd.getDate() + maxDays - 1);
        if (currentEnd > endDate) currentEnd = endDate;
        
        chunks.push({ start: new Date(currentStart), end: new Date(currentEnd) });
        currentStart = new Date(currentEnd);
        currentStart.setDate(currentStart.getDate() + 1);
    }
    
    return chunks;
}
```

---

## 📐 Пріоритети реалізації

### Пріоритет 1 (Критично):
1. ✅ Кнопка 60.1 на всю ширину
2. ✅ Прогрес для 61.1 (детальний)
3. ✅ Глобальний прогрес-бар

### Пріоритет 2 (Важливо):
4. ✅ Завантаження всього періоду (автоматичне розбиття)
5. ✅ Можливість відмінити процес

### Пріоритет 3 (Додатково):
6. ⏸️ Покращення візуального дизайну
7. ⏸️ Додаткові статистики про синхронізацію

---

## 🎨 Візуальні покращення

### Глобальний прогрес-бар:
- Розмір: висота ~40-50px, на всю ширину
- Кольори: бренд-кольори (brand-blue для прогресу)
- Позиція: sticky top або bottom
- Анімація: smooth transition при появі/зникненні

### Кнопки синхронізації:
- Великі, зрозумілі кнопки
- Іконки для кращого UX
- Disabled стан під час активного процесу

---

## ✅ Перевірка перед реалізацією

**Питання для уточнення:**
1. ✅ Період 1095 днів підтверджено? (3 роки від поточної дати назад)
2. ✅ Обмеження 45 днів на один запит - правильне?
3. ✅ Глобальний прогрес-бар: верхня або нижня панель? (рекомендую верхню)
4. ✅ Можливість відмінити - потрібна? (рекомендую так)

---

## 📝 Деталізація кроків

### Крок 1: Базові UI покращення (30 хв)
- Змінити розмір кнопки 60.1
- Додати детальний прогрес для 61.1

### Крок 2: Розбиття періоду (1 год)
- Створити `splitPeriodIntoChunks`
- Додати кнопку "Завантажити весь період"
- Реалізувати послідовне завантаження чанків

### Крок 3: Глобальний прогрес (2 год)
- Створити Context
- Створити компонент GlobalSyncProgressBar
- Інтегрувати в layout
- Оновити sync actions для підтримки callback

### Крок 4: Відміна процесу (1 год)
- Додати можливість відмінити
- Обробка відміни в sync actions

---

**Загальний час реалізації: ~4-5 годин**

---

**Готово до затвердження!** ✅
