# Документація модуля Архів МД

## 📋 Огляд

Модуль `archive` відповідає за відображення, фільтрацію, сортування та експорт митних декларацій (МД). Підтримує два формати даних: список 60.1 (короткий) та список 61.1 (детальний).

## 📁 Структура файлів

```
src/app/dashboard/archive/
├── __tests__/                    # Тести
│   ├── utils.test.ts            # Тести утиліт
│   ├── filter-utils.test.ts      # Тести фільтрації
│   └── export-utils.test.ts      # Тести експорту
├── components/
│   └── archive-statistics.tsx    # Компонент статистики
├── types.ts                      # TypeScript типи
├── constants.ts                  # Константи (статуси, налаштування)
├── utils.ts                      # Helper-функції
├── filter-utils.ts               # Логіка фільтрації
├── export-utils.ts               # Функції експорту в Excel
├── page-client.tsx               # Основний компонент (UI + логіка)
└── page.tsx                      # Server component (завантаження даних)
```

---

## 🔧 Основні модулі

### `types.ts`
Визначає TypeScript типи для декларацій та UI стану.

**Ключові типи**:
- `Declaration` - базова декларація з бази даних
- `DeclarationWithRawData` - декларація з rawData для list60
- `ViewMode` - режим відображення (table/cards/compact)
- `ActiveTab` - активна вкладка (list60/list61)
- `SortDirection` - напрямок сортування

### `utils.ts`
Helper-функції для роботи з даними декларацій.

**Функції**:
- `getRawData()` - безпечний доступ до rawData
- `formatRegisteredDate()` - форматування дат
- `decodeWindows1251()` - декодування Windows-1251 → UTF-8
- `getMDNumber()` - отримання номера МД

**Детальніше**: Див. JSDoc коментарі в файлі.

### `filter-utils.ts`
Логіка фільтрації декларацій для списку 60.1.

**Функції**:
- `filterDeclarations60()` - фільтрує декларації за заданими критеріями

**Підтримувані фільтри**:
- Статус (включаючи спеціальний "cleared")
- Діапазон дат
- Тип декларації (підтримує множинні значення)
- Митниця
- Текстовий пошук

**Детальніше**: Див. JSDoc коментарі в файлі.

### `export-utils.ts`
Функції експорту декларацій в Excel.

**Функції**:
- `exportToExcel()` - базовий експорт (один рядок на декларацію)
- `exportExtendedToExcel()` - розширений експорт (один рядок на товар)

**Формати**:
- **list60**: 7 колонок (базові дані)
- **list61**: 13 колонок (детальні дані)
- **Розширений**: Динамічні колонки, розрахунки в UAH/USD

**Детальніше**: Див. JSDoc коментарі в файлі.

### `page-client.tsx`
Основний компонент з UI та бізнес-логікою.

**Основні useMemo хуки**:
- `declarationsWithRawData` - парсить XML для list60
- `declarationsWithDetails` - підготовлює дані для list61
- `filteredDocs60` / `filteredDocs61` - фільтрує декларації
- `statistics` - розраховує статистику
- `sortedDocs` - сортує декларації
- `paginatedDocs` / `paginatedGroupedDocs` - пагінація

**Детальніше**: Див. JSDoc коментарі в коді.

### `components/archive-statistics.tsx`
Компонент відображення статистики.

**Функціональність**:
- Загальна статистика (кількість, статуси, вартості)
- Топ-10 списки (відправники, отримувачі, коди УКТЗЕД, тощо)
- Фільтрація по кліку на елементи
- Налаштування видимості блоків

---

## 🔄 Потік даних

### Завантаження даних
1. `page.tsx` (server) завантажує декларації з бази даних
2. Передає в `page-client.tsx` (client)

### Обробка даних
1. **Парсинг XML** (`declarationsWithRawData`):
   - Для list60: витягує rawData з JSON/XML
   - Для list61: парсить повний XML через `mapXmlToDeclaration`

2. **Підготовка даних** (`declarationsWithDetails`):
   - Пріоритет: mappedData з XML → mappedData з summary → null

3. **Фільтрація** (`filteredDocs60` / `filteredDocs61`):
   - Застосовує фільтри послідовно (AND логіка)
   - Нормалізує дані (типи декларацій, дати)

4. **Статистика** (`statistics`):
   - Групує по сутностях (Map для швидкості)
   - Сортує та обмежує до топ-10

5. **Сортування** (`sortedDocs`):
   - Підтримує різні колонки та напрямки
   - Обробляє відсутні значення

6. **Пагінація** (`paginatedDocs`):
   - Розбиває на сторінки
   - Підтримує групування за датами

---

## 🎯 Ключові особливості

### Парсинг XML
- **JSON формат**: `{ data60_1: {...}, data61_1: {...} }`
- **XML формат**: Прямий XML (fallback через regex)
- **Декодування**: Windows-1251 → UTF-8 для кирилиці

### Фільтрація
- **Множинні значення**: Тип декларації через кому ("ІМ,ЕК")
- **Нормалізація**: Уникає дублікатів через нормалізацію рядків
- **Пошук**: Регістронезалежний, по різним полям

### Статистика
- **Групування**: Використовує Map для швидкості
- **Уникає подвійного підрахунку**: Перевіряє наявність mappedData
- **Топ-10**: Сортує за кількістю, обмежує до 10

### Експорт
- **Базовий**: Один рядок на декларацію
- **Розширений**: Один рядок на товар, з розрахунками
- **Розрахунки**: 
  - `invoiceValueUah = goods.price * header.exchangeRate`
  - `invoiceValueUsd = invoiceValueUah / usdRate` (курс НБУ)

---

## 📝 Приклади використання

### Фільтрація
```ts
import { filterDeclarations60 } from './filter-utils';

const filtered = filterDeclarations60(declarations, {
  status: 'cleared',
  dateFrom: '2025-01-01',
  customsOffice: 'Митниця1',
  searchTerm: 'MRN123'
});
```

### Експорт
```ts
import { exportToExcel, exportExtendedToExcel } from './export-utils';

// Базовий експорт
await exportToExcel(sortedDocs, 'list60');

// Розширений експорт
await exportExtendedToExcel(sortedDocs, 'list61', exportColumns);
```

### Утиліти
```ts
import { getRawData, formatRegisteredDate, getMDNumber } from './utils';

const rawData = getRawData(declaration);
const date = formatRegisteredDate(rawData?.ccd_registered);
const mdNumber = getMDNumber(rawData, declaration.mrn);
```

---

## 🧪 Тестування

Всі критичні функції покриті тестами:
- `utils.test.ts` - 18 тестів
- `filter-utils.test.ts` - 13 тестів
- `export-utils.test.ts` - 4 тести

**Запуск тестів**:
```bash
npm test
```

---

## 🔍 Детальна документація

Для детальної інформації про функції див. JSDoc коментарі в коді:
- `utils.ts` - детальні описи helper-функцій
- `filter-utils.ts` - опис логіки фільтрації
- `export-utils.ts` - опис форматів експорту
- `page-client.tsx` - опис useMemo хуків та логіки

---

## 📚 Додаткові ресурси

- `ARCHIVE_EVALUATION.md` - оцінка функціональності
- `TESTING_SUMMARY.md` - підсумок тестування
- `EXCHANGE_RATES_SYNC.md` - синхронізація курсів валют

---

**Останнє оновлення**: 2025-01-16
