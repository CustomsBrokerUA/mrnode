# Гайд по тестуванню в проекті MRNode

## 🎯 Для чого потрібні тести?

Тести - це автоматичні перевірки коду, які допомагають:

### 1. **Знаходити помилки до випуску**
- Виявлення багів на ранніх етапах
- Запобігання регресіям при змінах
- Перевірка роботи коду в різних сценаріях

### 2. **Забезпечувати надійність**
- Критична логіка (шифрування, парсинг XML) працює правильно
- API інтеграції обробляють помилки коректно
- Дані обробляються без втрати інформації

### 3. **Служити документацією**
- Тести показують, як має працювати функція
- Приклади використання в коді
- Опис очікуваної поведінки

### 4. **Спрощувати рефакторинг**
- Можна сміливо змінювати код, знаючи що тести перевірять коректність
- Покриття тестами дає впевненість у змінах

## 🛠️ Як працюють тести?

### Фреймворк: Vitest

Використовуємо **Vitest** - сучасний фреймворк для тестування JavaScript/TypeScript:

```bash
npm test              # Запустити всі тести
npm test -- --run     # Запустити один раз (без watch mode)
npm test -- file.ts   # Запустити конкретний файл
```

### Структура тесту

Базовий приклад:

```typescript
import { describe, it, expect } from 'vitest';

describe('Назва модуля', () => {
    it('має виконувати певну дію', () => {
        // Arrange (підготовка) - створюємо тестові дані
        const input = 'тестовий рядок';
        
        // Act (дія) - викликаємо функцію
        const result = encrypt(input);
        
        // Assert (перевірка) - перевіряємо результат
        expect(result).toBeDefined();
        expect(result).not.toBe(input);
    });
});
```

### Ключові концепції

#### 1. **describe()** - групує тести
```typescript
describe('encrypt function', () => {
    // Всі тести для encrypt
});
```

#### 2. **it() або test()** - окремий тест
```typescript
it('має шифрувати текст', () => {
    // Тестова логіка
});
```

#### 3. **expect()** - перевірки
```typescript
expect(result).toBe(5);                    // Точне значення
expect(result).toBeDefined();              // Не undefined
expect(array).toHaveLength(3);             // Довжина масиву
expect(object).toHaveProperty('name');     // Властивість об'єкта
expect(fn).toThrow();                      // Функція кидає помилку
```

#### 4. **beforeEach/afterEach** - налаштування
```typescript
beforeEach(() => {
    // Виконується перед кожним тестом
    vi.clearAllMocks();
});

afterEach(() => {
    // Виконується після кожного тесту
    vi.restoreAllMocks();
});
```

## 📝 Типи тестів в проекті

### 1. Unit тести (одиничні)

Тестують окремі функції або класи в ізоляції.

**Приклад:** `crypto.test.ts`
```typescript
describe('encrypt function', () => {
    it('should encrypt a simple string', async () => {
        const text = 'hello world';
        const encrypted = await encrypt(text);
        
        expect(encrypted).toBeTypeOf('string');
        expect(encrypted.length).toBeGreaterThan(text.length);
    });
});
```

**Що тестується:**
- Функція `encrypt()` працює коректно
- Результат - рядок
- Зашифрований текст довший за оригінал

### 2. Мокування (Mocks)

Замінюємо зовнішні залежності (API, база даних) на фейкові об'єкти.

**Приклад:** `customs-api.test.ts`
```typescript
// Мокуємо axios
vi.mock('axios');
const mockedAxios = vi.mocked(axios, true);

// Мокуємо AdmZip
vi.mock('adm-zip', () => {
    class MockAdmZip {
        constructor(buffer: Buffer) {}
        getEntries() {
            return mockGetEntries();
        }
    }
    return { default: MockAdmZip };
});

// У тесті
mockedAxios.post.mockResolvedValueOnce({
    data: { messageBody: 'base64data' }
});
```

**Навіщо мокувати:**
- Не потрібно реальне API (швидко, не залежить від мережі)
- Контроль над даними (можемо повернути будь-які дані)
- Тести працюють без налаштування зовнішніх сервісів

### 3. Тести React хуків

Тестують React хуки з використанням `@testing-library/react`.

**Приклад:** `useArchiveSorting.test.ts`
```typescript
import { renderHook } from '@testing-library/react';

describe('useArchiveSorting', () => {
    it('should sort by mdNumber ascending', () => {
        const docs = [
            mockDeclaration('1', '123/456/002'),
            mockDeclaration('2', '123/456/001'),
        ];

        const { result } = renderHook(() => useArchiveSorting({
            filteredDocs: docs,
            activeTab: 'list60',
            sortColumn: 'mdNumber',
            sortDirection: 'asc'
        }));

        expect(result.current.sortedDocs[0].mrn).toBe('123/456/001');
    });
});
```

**Що робить renderHook:**
- Створює React контекст для хука
- Викликає хук з переданими параметрами
- Повертає результат через `result.current`

## 🔍 Приклади з нашого проекту

### Тест шифрування (`crypto.test.ts`)

```typescript
it('should decrypt encrypted text correctly', async () => {
    // Arrange
    const originalText = 'this is a secret';
    
    // Act - шифруємо
    const encrypted = await encrypt(originalText);
    
    // Act - розшифровуємо
    const decrypted = await decrypt(encrypted);
    
    // Assert - результат має дорівнювати оригіналу
    expect(decrypted).toBe(originalText);
});
```

**Навіщо:** Перевіряємо, що шифрування працює коректно - зашифрований текст можна розшифрувати назад.

### Тест XML парсингу (`xml-mapper.test.ts`)

```typescript
it('should parse declaration with goods', () => {
    // Arrange - XML з товарами
    const xml = `<?xml version="1.0"?>
        <ccd>
            <ccd_goods>
                <item>
                    <ccd_31_01>Test Goods</ccd_31_01>
                </item>
            </ccd_goods>
        </ccd>`;
    
    // Act - парсимо XML
    const result = mapXmlToDeclaration(xml);
    
    // Assert - товари розпарсені
    expect(result?.goods).toBeDefined();
    expect(result?.goods?.length).toBeGreaterThan(0);
});
```

**Навіщо:** Перевіряємо, що складний XML парситься правильно, товари витягуються з вкладених структур.

### Тест API (`customs-api.test.ts`)

```typescript
it('should return error response when API call fails', async () => {
    // Arrange - мокуємо помилку API
    mockedAxios.post.mockRejectedValueOnce(new Error('Network error'));
    
    // Act - викликаємо метод
    const result = await service.getDeclarationsList(dateFrom, dateTo);
    
    // Assert - має бути обробка помилки
    expect(result.success).toBe(false);
    expect(result.error).toBeDefined();
});
```

**Навіщо:** Перевіряємо, що помилки API обробляються коректно, система не падає при проблемах з мережею.

### Тест сортування (`useArchiveSorting.test.ts`)

```typescript
it('should sort by invoiceValue descending', () => {
    // Arrange - декларації з різними значеннями
    const docs = [
        mockDeclaration61('1', 'Consignor A', 500),
        mockDeclaration61('2', 'Consignor B', 2000),
        mockDeclaration61('3', 'Consignor C', 1000),
    ];
    
    // Act - сортуємо
    const { result } = renderHook(() => useArchiveSorting({
        filteredDocs: docs,
        activeTab: 'list61',
        sortColumn: 'invoiceValue',
        sortDirection: 'desc'
    }));
    
    // Assert - порядок правильний
    expect(result.current.sortedDocs[0].mappedData.header.invoiceValue).toBe(2000);
    expect(result.current.sortedDocs[1].mappedData.header.invoiceValue).toBe(1000);
});
```

**Навіщо:** Перевіряємо логіку сортування - дані мають сортуватися правильно за числовими значеннями.

## 🎨 Патерни тестування

### 1. AAA (Arrange-Act-Assert)

```typescript
it('test example', () => {
    // Arrange - підготовка
    const input = 'test';
    
    // Act - дія
    const result = functionToTest(input);
    
    // Assert - перевірка
    expect(result).toBe('expected');
});
```

### 2. Мокування залежностей

```typescript
// Мокуємо перед використанням
vi.mock('external-library');

// В тесті використовуємо мок
mockFunction.mockReturnValue('fake result');
```

### 3. Тестові дані (Test Fixtures)

```typescript
const mockDeclaration = (id: string, status: string) => ({
    id,
    status,
    // ... інші поля
});

// Використання
const doc1 = mockDeclaration('1', 'CLEARED');
const doc2 = mockDeclaration('2', 'PROCESSING');
```

## 📊 Поточне покриття

**Загалом: 128 тестів**

- ✅ crypto.ts (22 тести) - шифрування/розшифрування
- ✅ xml-mapper.ts (18 тестів) - парсинг XML
- ✅ nbu-api.ts (15 тестів) - курси валют
- ✅ customs-api.ts (15 тестів) - API митниці
- ✅ useArchiveSorting.ts (10 тестів) - сортування
- ✅ useArchiveStatistics.ts (13 тестів) - статистика
- ✅ export-utils.ts (4 тести) - експорт Excel
- ✅ utils.ts (~20 тестів) - утиліти
- ✅ filter-utils.ts (~10 тестів) - фільтрація

## 🚀 Як запустити тести

```bash
# Всі тести
npm test

# Конкретний файл
npm test -- crypto.test.ts

# З покриттям
npm test -- --coverage

# У режимі огляду
npm test:ui
```

## 💡 Рекомендації

1. **Тестуйте критичну логіку першою**
   - Шифрування, парсинг, обробка даних

2. **Пишіть тести перед рефакторингом**
   - Впевненість, що після змін все працює

3. **Один тест = одна перевірка**
   - Легше знайти проблему

4. **Використовуйте описові назви**
   - `it('should encrypt text correctly')` ✅
   - `it('test1')` ❌

5. **Мокуйте зовнішні залежності**
   - API, база даних, файлова система

## 🎯 Висновок

Тести - це інвестиція в якість коду:
- **Швидше знаходимо баги**
- **Сміліше рефакторимо**
- **Код стає документацією**
- **Менше проблем в продакшені**

**Покриття критичної логіки: ~85%** ✅
