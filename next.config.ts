import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    allowedDevOrigins: ["milkily-unluminiferous-joziah.ngrok-free.dev"],
  },
};

export default nextConfig;
